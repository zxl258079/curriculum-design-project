# IMS滚动轴承剩余寿命预测系统 设计报告

## 1 项目概述
本次课程设计实现一套基于时序深度学习的滚动轴承剩余使用寿命预测原型系统。采用B/S架构，Python‑Flask作为Web后端，SQLite实现本地数据存储，LSTM时序网络完成寿命预测。
系统实现设备信息录入、轴承剩余寿命预测、预测结果存储、历史记录查询完整业务流程。数据集使用IMS辛辛那提大学轴承全生命周期振动数据，模拟轴承从正常运行逐步退化失效的过程。
本项目综合实践Python编程、数据库原理、Web开发、机器学习时序数据分析，全部代码与文档使用GitHub进行版本管理。

## 2 需求分析
参考《需求规格说明书》。
业务目标：
1. 管理轴承设备基础信息；
2. 输入时序数据，模型输出RUL剩余寿命，输出健康等级和运维建议；
3. 保存每一次预测，支持历史记录查询；
4. 本地单机网页运行，数据持久保存。

## 3 总体方案设计
### 3.1 整体架构 B/S
1. 前端：HTML+CSS+JavaScript，负责页面展示、表单输入、调用后端接口、表格渲染结果；
2. 后端：Flask，接收浏览器请求，调度数据预处理、模型预测、数据库增删改查；
3. 模型层：LSTM时序神经网络，接收时间窗口样本，输出剩余寿命预测值；
4. 数据层：SQLite文件数据库，保存设备与预测记录。

### 3.2 模块划分
1. 数据库初始化模块 `db_init.py`：创建设备表、预测记录表；
2. 数据预处理模块 `data_preprocess.py`：读取数据集，清洗、归一化、滑动窗口构造时序样本；
3. 模型训练模块 `train.py`：搭建LSTM网络，训练并保存模型权重到output；
4. 预测模块 `predict.py`：加载权重，输入时序样本得到RUL、健康状态、运维建议；
5. Web服务模块 `server.py`：实现3个API接口，对接前端与业务逻辑；
6. 前端页面 `templates/index.html`：页面交互。

## 4 详细设计
### 4.1 数据库设计
#### 设备表 device
|字段 |类型 |说明 |
|----|-----|-----|
|id |INTEGER |主键，自增，设备唯一编号 |
|equip_name |TEXT |设备名称 |
|equip_model |TEXT |设备型号 |
|create_time |TIMESTAMP |设备录入时间 |

#### 预测记录表 predict_record
|字段 |类型 |说明 |
|----|-----|-----|
|id |INTEGER |主键，自增记录ID |
|device_id |INTEGER |外键，关联设备id |
|pred_rul |REAL |预测剩余寿命RUL |
|health_status |TEXT |设备健康状态 |
|advice |TEXT |运维建议 |
|predict_time |TIMESTAMP |预测时间 |

### 4.2 后端接口设计
1. `/api/add_device` POST
接收设备名称、型号，插入device表，返回新生成设备id。

2. `/api/predict_rul` POST
接收device_id，调用predict模块得到预测结果，写入predict_record表，返回预测结果。

3. `/api/query_history` POST
有device_id就筛选该设备；为空则查询全部记录，组装数组返回前端渲染表格。

### 4.3 LSTM模型设计
输入：`[batch, time_window=30, feature=21]` 时序样本；
网络结构：一层LSTM隐藏层，隐藏维度64；全连接层输出1维RUL预测值；
损失函数：MSE均方误差；优化器Adam。

### 4.4 业务逻辑
1. 预处理：原始振动数据→缺失填充→归一化→滑动窗口截取30步长时序样本；
2. 训练：样本送入LSTM，迭代更新权重，保存`model.pth`；
3. 预测：输入一段时序，模型输出RUL，阈值判断健康状态与运维建议；
4. Web流程：浏览器表单提交 → Flask接口处理 → 数据库读写 → JSON返回页面展示。

## 5 开发与调试
### 5.1 开发环境
Python3.10，Flask，numpy，pandas，torch；浏览器Edge / Chrome。

### 5.2 开发步骤
1. 编写`db_init.py`，创建两张数据表；
2. 编写数据预处理脚本，处理数据集生成时序样本；
3. 编写`train.py`搭建LSTM，训练并保存模型权重；
4. 编写`predict.py`封装预测函数；
5. 编写`server.py`，完成Flask后端三个API；
6. 编写前端index.html页面，完成页面交互；
7. 本地运行，完整联调：录入设备→拿到设备编号→执行预测→查询历史记录。

### 5.3 遇到问题与解决
1. 数据集文件格式问题：原始文件逗号分隔，最初使用空格分隔符读取，导致读取列数错误；修改`sep=","`适配csv格式，成功解析数据集。
2. 前端JS请求报错：请求头缺少`Content‑Type:application/json`，添加请求头修复接口传参异常。
3. 文件目录错误：md文档放到src文件夹，GitHub无法预览，把全部md迁移到仓库根目录。

## 6 系统测试
### 6.1 功能测试用例
1. **设备录入测试**
输入设备名称、型号，点击录入；数据库新增一条设备记录，页面返回正确设备编号。

2. **寿命预测测试**
填入存在的设备编号，执行预测；页面输出RUL数值、健康状态、运维建议；数据库新增一条预测记录。

3. **历史查询测试**
①填入设备编号，查询该设备全部预测记录，表格展示；
②输入框留空，点击查询，展示全部设备的预测历史。

### 6.2 测试结论
系统全部业务功能正常，模块之间调用正常，满足课程设计需求。

## 7 总结与展望
### 7.1 总结
本课程设计完成IMS轴承剩余寿命预测B/S原型系统。
实现了数据集预处理、LSTM时序模型训练、SQLite数据库存储、Flask网页前后端交互整套流程。
熟悉Git与GitHub版本管理；掌握时序预测项目完整开发流程；理解预测性维护的基本思想；学会使用AI辅助开发，同时读懂代码、排查bug。

### 7.2 不足与展望
1. 当前模型比较简单，可以尝试增加网络层数，进一步提高预测精度；
2. 页面UI简陋，可以增加数据可视化图表，绘制寿命退化曲线；
3. 可以增加批量导入数据集、导出历史记录Excel等拓展功能。

## 参考文献
[1] 周志华.机器学习[M].清华大学出版社,2016.
[2] 黑马程序员.Python Web开发[M].人民邮电出版社
[3] IMS University of Cincinnati Bearing Dataset Technical Document
