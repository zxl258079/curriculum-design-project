# IMS轴承剩余寿命预测系统 需求规格说明书

## 1 引言
### 1.1 编写目的
本文档定义**IMS滚动轴承剩余使用寿命(RUL)预测系统**全部需求，作为系统设计、编码实现、测试验证的依据，用于课程设计开发与答辩评审。明确系统功能、性能、数据、接口、约束条件。

### 1.2 项目背景
滚动轴承是旋转机械设备的核心零部件，长期运行会发生磨损、疲劳剥落，突发故障会造成设备停机与经济损失。
传统维护分为事后维修、定期检修，无法实现预判故障。预测性维护依靠传感器采集振动时序数据，评估设备退化程度，输出剩余寿命，辅助运维决策。
本系统采用B/S架构，基于Python‑Flask后端，SQLite本地数据库，实现轴承设备管理、寿命预测、历史记录查询整套原型系统。

### 1.3 参考资料
[1] 课程设计任务书
[2] 周志华.机器学习[M].清华大学出版社
[3] IMS辛辛那提大学轴承数据集公开说明文档

## 2 总体描述
### 2.1 产品目标
实现浏览器直接访问的预测原型系统：完成设备信息录入、剩余寿命预测、预测结果持久化存储、历史记录查询；不需要额外安装客户端，本地单机运行。

### 2.2 运行环境
- 操作系统：Windows10 / Windows11
- Python版本：Python3.8及以上
- 第三方库：Flask、numpy、pandas、torch
- 数据库：SQLite（文件数据库，无需额外安装服务）
- 客户端：Chrome、Edge主流浏览器

### 2.3 用户特征
本系统面向课程设计演示使用，用户为学生；操作简单，不需要深度学习算法专业背景，仅做页面表单操作。

## 3 功能需求
### 3.1 设备信息录入模块
1. 用户输入设备名称、设备型号；
2. 提交表单，后端将信息存入SQLite设备数据表；
3. 录入成功自动生成唯一设备编号并返回前端展示。

### 3.2 剩余寿命预测模块
1. 输入已经存在的设备编号，发起预测请求；
2. 后端加载训练完成的时序预测模型，输入时序样本计算RUL剩余寿命；
3. 根据RUL数值划分健康等级，自动生成对应的运维建议；
4. 将设备编号、RUL、健康状态、运维建议、预测时间保存到预测记录表；
5. 将预测结果返回网页页面展示。

>健康等级划分规则
- RUL＜40：故障风险高，尽快停机检修
- 40 ≤ RUL ＜ 80：状态一般，加强巡检
- RUL ≥ 80：状态良好，正常运行

### 3.3 历史记录查询模块
1. 支持两种查询模式：按设备编号查询单台设备记录；输入为空则查询全部预测记录；
2. 查询结果以表格形式展示：记录ID、设备名称、设备型号、RUL、健康状态、运维建议、预测时间。

### 3.4 数据预处理模块
1. 读取振动时序数据集，完成缺失值填充、异常过滤；
2. 传感器特征归一化；
3. 使用滑动窗口生成模型可输入的时序样本；
4. 输出处理后的样本文件，供给模型训练使用。

## 4 非功能需求
### 4.1 性能需求
1. 单机单用户使用；页面接口响应时间 ≤2秒；
2. 数据库可保存上百条设备与预测记录，满足课程演示。

### 4.2 可靠性需求
1. SQLite文件持久化存储，程序关闭数据不会丢失；
2. 非法输入做基础容错，程序不会直接崩溃退出。

### 4.3 易用性需求
1. Web页面，表单+按钮操作，无需命令行操作业务功能；
2. 文字提示清晰，表格直观展示结果。

## 5 数据需求
### 5.1 设备表 device
|字段 |类型 |说明 |
|----|-----|-----|
|id |INTEGER |主键，自增，设备唯一编号 |
|equip_name |TEXT |设备名称 |
|equip_model |TEXT |设备型号 |
|create_time |TIMESTAMP |设备录入时间 |

### 5.2 预测记录表 predict_record
|字段 |类型 |说明 |
|----|-----|-----|
|id |INTEGER |主键，自增记录ID |
|device_id |INTEGER |关联设备表id |
|pred_rul |REAL |预测剩余寿命RUL |
|health_status |TEXT |设备健康状态 |
|advice |TEXT |运维建议 |
|predict_time |TIMESTAMP |预测发生时间 |

### 5.3 数据集
IMS轴承振动时序数据集，存放于项目`data/`目录，用于模型训练。

## 6 接口需求（后端API接口）
1. `/api/add_device` POST
>功能：新增设备；入参equip_name、equip_model；返回code、device_id设备编号。

2. `/api/predict_rul` POST
>功能：执行剩余寿命预测；入参device_id；返回rul、health_status、advice。

3. `/api/query_history` POST
>功能：查询预测历史；入参device_id可为空；返回记录列表数组。

## 7 其他约束
1. 全部代码、文档、数据集、AI对话记录统一托管GitHub仓库；
2. 目录结构规范：源代码放在src目录，文档放在仓库根目录；
3. 可以本地一键运行，不需要部署到云服务器。
