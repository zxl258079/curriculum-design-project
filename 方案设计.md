# 方案设计

## 一、功能需求分析
### 1.1 功能性需求
1. **数据集导入解析模块**
    支持读取IMS轴承数据集原始文本文件或采样CSV文件，解析时间戳、多通道振动加速度传感器采样值，完成基础数据格式校验。

2. **时序数据预处理模块**
    - 噪声抑制：滑动平均滤波处理振动高频噪声；
    - 异常值剔除：采用3‑σ准则剔除传感器跳变、电磁干扰产生的异常采样点；
    - 特征工程：提取振动信号时域特征（均值、方差、均方根RMS、峭度、峰值因子、波形因子）；可选FFT快速傅里叶变换提取频域特征；
    - 时序样本构造：采用滑动窗口对连续长时序进行切片，生成固定长度的时序样本；
    - 数据标准化：Z‑score标准化消除量纲差异；
    - 数据集划分：按时间顺序划分训练集、验证集、测试集，避免时序数据时间穿越问题。

3. **深度学习预测模块**
    - 可配置LSTM网络结构：隐藏层层数、神经元数量、Dropout正则化系数；
    - 训练配置可调：学习率、批次大小batch size、最大迭代轮数、早停策略；
    - 模型训练、保存权重、加载预训练权重；
    - 输入时序样本，输出轴承剩余使用寿命RUL预测值。

4. **健康状态分级与维护决策模块**
    根据RUL预测结果设置阈值，划分三级健康状态：
    - 正常运行：剩余寿命充足，无需维护；
    - 早期退化预警：开始出现性能退化，建议定期加强监测；
    - 紧急维护预警：剩余寿命不足，需要尽快停机检修；
    输出维护优先级排序与简单运维建议文本。

5. **后端API服务模块**
    使用FastAPI提供RESTful接口：
    - 数据上传与预处理接口
    - 模型训练启动接口
    - 寿命预测推理接口
    - 历史预测结果查询接口
    SQLite轻量数据库持久存储预处理样本、预测记录、健康评估结果。

6. **前端可视化UI模块**
    - 振动原始时序波形图展示；
    - 特征指标变化趋势曲线；
    - 轴承退化趋势、RUL预测结果可视化；
    - 健康状态仪表盘、预警提示、维护建议展示页面；
    - 简单的系统参数、模型参数查看界面。

7. **项目过程管理模块**
    Git小步提交，保留版本演进；建立`prompt`目录按阶段保存AI交互记录，留存Vibe Coding全过程档案。

### 1.2 非功能性需求
1. 部署环境：可在普通Windows/Linux个人电脑完成部署运行，不依赖云服务器；
2. 性能：单条样本模型推理时间＜10 s；完整数据集训练可在数小时内完成；
3. 可调试性：代码注释完整，关键模块可单独运行测试；
4. 可复现性：数据集来源、预处理步骤、模型参数全部文档化；
5. 过程可追溯：完整保留Git提交记录与AI提示词日志，用于报告撰写与答辩。

## 二、方案论证
装备剩余寿命预测主流可选技术路线对比：
1. **传统机器学习（SVM、随机森林、梯度提升树）**
    优点：训练速度快；缺点：高度依赖人工设计高质量特征，自动捕捉长时序退化依赖的能力有限，泛化能力一般。

2. **卷积神经网络CNN**
    优点：擅长提取局部模式；缺点：更适合空间数据，对超长时序的长距离依赖建模能力弱，不是时序退化任务最优选择。

3. **LSTM长短期记忆循环神经网络**
    专门设计解决时序梯度消失问题，可以学习振动信号在数小时、数天尺度上缓慢退化的长期依赖，是工业装备RUL预测领域应用最广泛的基准模型之一，成熟度高、参考文献丰富，**本项目选用LSTM方案**。

>数据集选择论证：放弃热门的C‑MAPSS、CWRU、NEU、AI4I‑2020，选用IMS辛辛那提轴承数据集，既保持了“时序退化‑寿命预测”典型工业任务，又规避选题重复风险，完全满足课程设计选题要求。

## 三、方案规划
### 3.1 技术栈选型
- **后端语言与框架**：Python 3.10+，FastAPI
- **深度学习框架**：PyTorch
- **数据处理库**：NumPy、Pandas、SciPy（信号处理、滤波、FFT）
- **数据库**：SQLite（轻量文件数据库，无需额外安装服务）
- **前端**：Vue3 + Vite + Element‑Plus + ECharts
- **版本控制**：Git + GitHub
- **AI辅助开发Harness与模型**：DeepSeek‑v4、GLM‑5.3；采用Vibe Coding提示词工程驱动开发

### 3.2 外部资源、数据集来源
数据集名称：IMS University of Cincinnati Bearing Dataset
官方镜像地址：https://data.nist.gov/od/id/mds2-13WL-B74Z

>完整原始数据集约700 MB，GitHub仓库容量有限，**仓库不上传全部原始数据**，`data/`目录存放数据集说明文档，可选少量采样`sample_data.csv`用于代码调试演示，完整数据由使用者自行下载。

### 3.3 仓库目录规划
# 方案设计

## 一、功能需求分析
### 1.1 功能性需求
1. **数据集导入解析模块**
    支持读取IMS轴承数据集原始文本文件或采样CSV文件，解析时间戳、多通道振动加速度传感器采样值，完成基础数据格式校验。

2. **时序数据预处理模块**
    - 噪声抑制：滑动平均滤波处理振动高频噪声；
    - 异常值剔除：采用3‑σ准则剔除传感器跳变、电磁干扰产生的异常采样点；
    - 特征工程：提取振动信号时域特征（均值、方差、均方根RMS、峭度、峰值因子、波形因子）；可选FFT快速傅里叶变换提取频域特征；
    - 时序样本构造：采用滑动窗口对连续长时序进行切片，生成固定长度的时序样本；
    - 数据标准化：Z‑score标准化消除量纲差异；
    - 数据集划分：按时间顺序划分训练集、验证集、测试集，避免时序数据时间穿越问题。

3. **深度学习预测模块**
    - 可配置LSTM网络结构：隐藏层层数、神经元数量、Dropout正则化系数；
    - 训练配置可调：学习率、批次大小batch size、最大迭代轮数、早停策略；
    - 模型训练、保存权重、加载预训练权重；
    - 输入时序样本，输出轴承剩余使用寿命RUL预测值。

4. **健康状态分级与维护决策模块**
    根据RUL预测结果设置阈值，划分三级健康状态：
    - 正常运行：剩余寿命充足，无需维护；
    - 早期退化预警：开始出现性能退化，建议定期加强监测；
    - 紧急维护预警：剩余寿命不足，需要尽快停机检修；
    输出维护优先级排序与简单运维建议文本。

5. **后端API服务模块**
    使用FastAPI提供RESTful接口：
    - 数据上传与预处理接口
    - 模型训练启动接口
    - 寿命预测推理接口
    - 历史预测结果查询接口
    SQLite轻量数据库持久存储预处理样本、预测记录、健康评估结果。

6. **前端可视化UI模块**
    - 振动原始时序波形图展示；
    - 特征指标变化趋势曲线；
    - 轴承退化趋势、RUL预测结果可视化；
    - 健康状态仪表盘、预警提示、维护建议展示页面；
    - 简单的系统参数、模型参数查看界面。

7. **项目过程管理模块**
    Git小步提交，保留版本演进；建立`prompt`目录按阶段保存AI交互记录，留存Vibe Coding全过程档案。

### 1.2 非功能性需求
1. 部署环境：可在普通Windows/Linux个人电脑完成部署运行，不依赖云服务器；
2. 性能：单条样本模型推理时间＜10 s；完整数据集训练可在数小时内完成；
3. 可调试性：代码注释完整，关键模块可单独运行测试；
4. 可复现性：数据集来源、预处理步骤、模型参数全部文档化；
5. 过程可追溯：完整保留Git提交记录与AI提示词日志，用于报告撰写与答辩。

## 二、方案论证
装备剩余寿命预测主流可选技术路线对比：
1. **传统机器学习（SVM、随机森林、梯度提升树）**
    优点：训练速度快；缺点：高度依赖人工设计高质量特征，自动捕捉长时序退化依赖的能力有限，泛化能力一般。

2. **卷积神经网络CNN**
    优点：擅长提取局部模式；缺点：更适合空间数据，对超长时序的长距离依赖建模能力弱，不是时序退化任务最优选择。

3. **LSTM长短期记忆循环神经网络**
    专门设计解决时序梯度消失问题，可以学习振动信号在数小时、数天尺度上缓慢退化的长期依赖，是工业装备RUL预测领域应用最广泛的基准模型之一，成熟度高、参考文献丰富，**本项目选用LSTM方案**。

>数据集选择论证：放弃热门的C‑MAPSS、CWRU、NEU、AI4I‑2020，选用IMS辛辛那提轴承数据集，既保持了“时序退化‑寿命预测”典型工业任务，又规避选题重复风险，完全满足课程设计选题要求。

## 三、方案规划
### 3.1 技术栈选型
- **后端语言与框架**：Python 3.10+，FastAPI
- **深度学习框架**：PyTorch
- **数据处理库**：NumPy、Pandas、SciPy（信号处理、滤波、FFT）
- **数据库**：SQLite（轻量文件数据库，无需额外安装服务）
- **前端**：Vue3 + Vite + Element‑Plus + ECharts
- **版本控制**：Git + GitHub
- **AI辅助开发Harness与模型**：DeepSeek‑v4、GLM‑5.3；采用Vibe Coding提示词工程驱动开发

### 3.2 外部资源、数据集来源
数据集名称：IMS University of Cincinnati Bearing Dataset
官方镜像地址：https://data.nist.gov/od/id/mds2-13WL-B74Z

>完整原始数据集约700 MB，GitHub仓库容量有限，**仓库不上传全部原始数据**，`data/`目录存放数据集说明文档，可选少量采样`sample_data.csv`用于代码调试演示，完整数据由使用者自行下载。

### 3.3 仓库目录规划

## 四、技术路线
1. **数据层**
IMS原始振动数据 → 读取解析 → 滑动平均滤波降噪 → 3‑σ异常值剔除 → 时域/频域特征提取 → 滑动窗口时序切片 → Z‑score标准化 → 划分训练、验证、测试时序样本集。

2. **算法层**
时序样本输入LSTM网络 → 前向传播计算损失 → 反向传播更新权重，加入Dropout防止过拟合；采用早停策略防止过拟合；保存最优模型权重；加载权重对测试样本推理，输出RUL预测值。

3. **服务层**
将数据预处理、模型推理封装为FastAPI Web接口；SQLite数据库存储预测结果与运维记录；接收前端HTTP请求，返回JSON格式结果。

4. **应用层**
Vue前端调用后端API获取振动数据、特征曲线、RUL预测结果；ECharts绘制波形图、趋势图、健康状态仪表盘；向用户展示预警等级与维护建议。

5. **工程过程层（Vibe Coding）**
需求规格文档 → AI辅助生成初始代码 → 人工审查代码逻辑 → 单元测试、发现AI生成错误 → 用提示词修正与迭代；每个开发阶段备份AI对话日志；Git频繁小步提交，记录每一步改动。

## 五、计划安排（按10天课程设计周期）
1. D1‑D2：GitHub仓库完善，IMS数据集下载与调研，数据预处理代码开发调试；
2. D3‑D4：LSTM时序预测模型搭建、训练、调参，完成模型验证；
3. D5：后端FastAPI接口开发，数据库设计与实现；
4. D6‑D7：Vue前端页面、可视化图表开发，前后端联调；
5. D8：系统整体集成、功能测试、bug修复，完善自动化简易测试；
6. D9：整理全部过程档案（Git提交记录、prompt日志），撰写完整课程设计报告；
7. D10：录制3分钟系统演示视频，制作答辩PPT，准备答辩随机代码讲解。

## 六、参考文献
[1] 张智海等.制造智能技术基础[M].北京:清华大学出版社,2022.
[2] 杨杰.人工智能基础[M].北京:机械工业出版社,2025.
[3] Lee J, Qiu H, Yu G, et al. IMS Bearing Data Set[EB/OL]. University of Cincinnati, 2007.
[4] 王鹏,李宏宇.基于LSTM的滚动轴承剩余寿命预测方法[J].组合机床与自动化加工技术,2024(05):78‑82.
[5] 赵春晖.工业时序信号特征提取与装备健康评估[M].北京:机械工业出版社,2023.
[6] 刘大同,郭小萍,彭宇.装备剩余使用寿命预测技术综述[J].自动化学报,2021,47(01):1‑21.
